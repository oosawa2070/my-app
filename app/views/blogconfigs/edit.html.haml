%h1 Blogconfigs#index
%p ※ブログ設定の編集
- if @blogconfig.errors.any?
  %ul
    - @blogconfig.errors.full_message.each do |err|
      %li= err
%table
  = form_for(@blogconfig, url:{controller:'blogconfigs', action:'edit'}) do |form|
    %tr
      %th Title
      %td= form.text_field :title
    %tr
      %th Subtitle
      %td= form.text_field :subtitle
    %tr
      %th Stylename
      %td= form.text_field :stylename
    %tr
      %th
      %td= form.submit '送信'
