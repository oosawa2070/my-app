%h1 Bloggenres#edit
%p
  ※「#{@bloggenre.name}」ジャンルの編集
- if @bloggenre.errors.any?
  %ul
    - @bloggenre.errors.full_message.each do |err|
      %li= err
%table
  = form_for(@bloggenre, url:{controller:'bloggenres', action:'edit'}) do |form|
    %tr
      %th Title
      %td= form.text_field :name
    %tr
      %th Subtitle
      %td= form.text_field :memo
    %tr
      %th
      %td= form.submit "送信"
